// Map configuration for static map provider
// If googleApiKey is set, DriveCard will use Google Static Maps; otherwise OSM static map is used.

const mapConfig = {
  googleApiKey: null, // e.g. 'AIza...'
  defaultZoom: 13,
  defaultSize: '600x300',
};

export default mapConfig;
